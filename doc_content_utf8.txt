455003439795660007056755activacion practicos360000activacion practicoscentercenter95000950004550034397952500266700Proyecto propuesto en las prÃ¡cticas de Globomatik para recursos humanos3700030000Proyecto propuesto en las prÃ¡cticas de Globomatik para recursos humanos44000332613025002667004000070000455003439795690007377430370000455003439795350003742055Portal de GestiÃ³n de RRHHProyecto tocho3600028000Portal de GestiÃ³n de RRHHProyecto tochoContenidoNo se encontraron entradas de tabla de contenido.1.- Backend(Tablas) -Users-departments-roles-documents-vacations-holiday_dates-vacation_balances-activity_logs Creamos los modelos de las tablas principales con el comando de siempre, con -mc para las migraciones y los controles y al final aÃ±adimos â€“api para generar los controladores sin las vistas HTML(crÃ©ate, edit), preparÃ¡ndolo exclusivamente para devolver un JSON. Ej: php artisan make:model VacationBalance -m -c â€“api Como me recomiendan paquetes externos (Roles y logs) tenemos que hacerlo de otra manera porque me pide usar paquetes de Spatie. No se crean los modelos a mano, sino que se instalan paquetes y publicar sus tablas Para roles y permisos composer require spatie/laravel-permission php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider" Para los logs de auditorÃ­a composer require spatie/laravel-activitylog php artisan vendor:publish --provider="Spatie\Activitylog\ActivitylogServiceProvider" --tag="activitylog-migrations" 1.1.- Uso de Spatie (ExplicaciÃ³n IA) spatie/laravel-permission (GestiÃ³n de Roles y Permisos) En lugar de que tÃº tengas que programar desde cero la lÃ³gica de "quiÃ©n puede hacer quÃ©" y crear mÃºltiples tablas complejas, este paquete te lo da hecho. Para quÃ© sirve en tu proyecto: Te permite asignar autorizaciones nativas con los roles requeridos: admin, hr_director, y employee. Tablas que genera: Crea automÃ¡ticamente las tablas roles y model_has_roles (entre otras). Ejemplo prÃ¡ctico: Gracias a esto, con una simple lÃ­nea de cÃ³digo como $user->hasRole('hr_director') podrÃ¡s verificar si un usuario tiene permiso para aprobar o rechazar vacaciones. spatie/laravel-activitylog (AuditorÃ­a y Trazabilidad) El documento exige cumplir con normativas estrictas (como el RGPD), por lo que necesitas saber exactamente quÃ© hace cada usuario en el sistema. Para quÃ© sirve en tu proyecto: ActÃºa como una "caja negra". Guarda un registro (Activity log) de todas las acciones importantes para tener trazabilidad. Tablas que genera: Crea automÃ¡ticamente la tabla activity_logs para la auditorÃ­a. Ejemplo prÃ¡ctico: Si un administrador sube una nÃ³mina, o un Director de RRHH aprueba unas vacaciones, este paquete anota en la base de datos: QuiÃ©n lo hizo, cuÃ¡ndo lo hizo, y quÃ© datos cambiaron exactamente. En resumen: Spatie te evita reinventar la rueda . Al usar sus paquetes, delegas la seguridad de los roles y la complejidad de la auditorÃ­a a cÃ³digo que ya ha sido probado por miles de empresas en todo el mundo. 1.2.- Relleno de migraciones y subida a la BD Rellenamos las migraciones segÃºn nos pide el pdf. Cuando las acabamos debemos de linkear la BD con nuestro proyecto y posteriormente hacer un migrate:fresh de esos. Cuando veamos las tablas en phpMyAdmin ya estarÃ­a eso. 1.3.-Models Cuando creamos las tablas con sus columnas en las migraciones debemos de poner los campos en los modelos para indicar que se pueden rellenar y tambiÃ©n debemos de poner el tipo de relaciones(has many y todas las paranoias esas). Mas tarde hacemos un seeder para tener un usuario admin. 1.4.-Seguimos Creando Creamos un controller para usar laravel Sanctum para las validaciones que tenemos que hacer a la hora de iniciar sesiÃ³n. Creamos el auth controller: php artisan make:controller Api/V1/AuthController Lo rellenamos como debemos de hacer y ponemos lo necesario en api. 1.5.- Dependencias y ConfiguraciÃ³n API-first Como el frontend va a ser totalmente desacoplado en React, no instalamos ni Livewire ni Blade. Solo instalamos paquetes centrados en la API, rendimiento y documentaciÃ³n:Para instalar Sanctum y generar api.php: php artisan install:apiPara documentar la API con OpenAPI: composer require --dev knuckleswtf/scribe y luego php artisan vendor:publish --tag=scribe-configPara calidad de cÃ³digo y detectar errores: composer require --dev barryvdh/laravel-debugbar, composer require --dev nunomaduro/larastan y friendsofphp/php-cs-fixer.1.6.- MÃ³dulo de Empleados (La TrÃ­ada API-first) Para seguir el estÃ¡ndar profesional API-first, dividimos la lÃ³gica de cada mÃ³dulo en 3 partes ejecutando estos comandos:FormRequest (Para validar la entrada): php artisan make:request Api/V1/StoreEmployeeRequestAPI Resource (Para formatear el JSON de salida y ocultar datos sensibles): php artisan make:resource V1/EmployeeResourceControlador API (El cerebro que conecta todo): php artisan make:controller Api/V1/EmployeeController --apiNota de resoluciÃ³n de errores: Al crear el borrado de empleados (destroy), fallaba el $this->authorize(). Lo solucionamos usando directamente la funciÃ³n de Spatie a travÃ©s de la Request del usuario: if (!$request->user() || !$request->user()->hasRole('admin')) { return error 403 }.1.7.- Rutas API y apiResource Actualizamos nuestro routes/api.php metiendo las rutas dentro del grupo protegido por Sanctum (Route::middleware('auth:sanctum')). En lugar de crear 5 rutas GET, POST, DELETE a mano, usamos un atajo de Laravel que genera todo el CRUD automÃ¡tico: Route::apiResource('employees', EmployeeController::class);.1.8.- ConfiguraciÃ³n de CORS Un paso crÃ­tico de seguridad para que el navegador no bloquee a React cuando intente conectarse al Laravel.Publicamos el archivo: php artisan config:publish corsEntramos a config/cors.php y aÃ±adimos los paths permitidos (['api/*', 'sanctum/csrf-cookie']), los orÃ­genes (http://localhost:3000 y 5173) y activamos supports_credentials => true.Finalmente, aÃ±adimos FRONTEND_URL=http://localhost:3000 en el archivo .env.1.9.- MÃ³dulo de Documentos y NÃ³minas Aplicamos la misma "TrÃ­ada API-first" (Request, Resource, Controller) para gestionar las nÃ³minas, contratos y otros certificados.Comandos base: php artisan make:controller Api/V1/DocumentController --api (junto con su FormRequest y su Resource).LÃ³gica de Seguridad: Los archivos PDF no se guardan en la carpeta pÃºblica (public/), sino en el almacenamiento privado del servidor (storage/app/documents). De esta forma, es imposible que alguien descargue una nÃ³mina adivinando la URL en el navegador.Para descargarlo, el frontend hace una peticiÃ³n a la API. El controlador comprueba si el usuario logueado es el dueÃ±o del documento o si tiene rol de RRHH/Admin. Si pasa el filtro, devuelve el archivo usando Storage::download().1.10.- MÃ³dulo de Vacaciones (El nÃºcleo de negocio) Este es el mÃ³dulo mÃ¡s complejo ya que incluye las reglas de negocio principales.Comandos base: Generamos VacationController, StoreVacationRequest y VacationResource.Regla de solapamientos: Antes de guardar una nueva solicitud en la BD, el controlador hace una consulta para asegurarse de que el empleado no tiene otra solicitud (pendiente o aprobada) que coincida en esas fechas.GestiÃ³n de estados: El empleado solicita las vacaciones y entran en estado pending. El empleado solo puede cancelar (canceled) si estÃ¡n en pendiente.Solo los usuarios con rol admin o hr_director pueden lanzar los mÃ©todos de approve o reject.Descuento automÃ¡tico: Al ejecutar la aprobaciÃ³n, el controlador busca el saldo de ese aÃ±o y le suma los dÃ­as gastados automÃ¡ticamente.1.11.- MÃ³dulo de Saldos de Vacaciones (Vacation Balances) Este mÃ³dulo alimenta la interfaz de React para decirle al empleado cuÃ¡ntos dÃ­as le quedan.Truco de Laravel: En lugar de crear los saldos a mano a final de aÃ±o, usamos el mÃ©todo firstOrCreate() en el controlador. Si un empleado consulta su saldo de este aÃ±o y no existe en la BD, Laravel lo crea en ese mismo instante asignÃ¡ndole los dÃ­as base por convenio (ej. 22 dÃ­as).En el VacationBalanceResource, calculamos los dÃ­as reales disponibles "al vuelo" sumando los dÃ­as generados y los arrastrados del aÃ±o anterior, y restando los consumidos. AsÃ­ React recibe el nÃºmero limpio y directo.1.12.- MÃ³dulo de Fechas Festivas (Calendario Laboral) Creamos el HolidayDateController y sus archivos asociados para que RRHH pueda registrar los dÃ­as festivos (Ã¡mbito nacional, de centro o personalizado). Esto es un requisito fundamental para que, al calcular cuÃ¡ntos dÃ­as gasta una solicitud de vacaciones, el sistema sepa quÃ© dÃ­as no debe restar del saldo (ademÃ¡s de los fines de semana).1.13.- SoluciÃ³n de Errores: El Controller Padre Al crear los controladores de la API dentro de Api/V1, nos daba un error de herencia (extends Controller). Esto ocurre porque en Laravel 11 el archivo controlador base a veces no se genera.SoluciÃ³n: Creamos manualmente el archivo raÃ­z en app/Http/Controllers/Controller.php definiÃ©ndolo como una clase abstracta vacÃ­a (abstract class Controller {}). Con esto, todos los controladores hijos heredan correctamente y desaparecen los fallos en el cÃ³digo.1.14.- GeneraciÃ³n de Datos de Prueba (Seeders y Factories) Para poder programar y probar el frontend en React (paginaciÃ³n, filtros, grÃ¡ficas), es imposible hacerlo con una base de datos vacÃ­a. Para solucionarlo, automatizamos la creaciÃ³n de datos:UserFactory: Modificamos la fÃ¡brica de usuarios de Laravel para que genere datos coherentes con nuestro sistema, incluyendo la generaciÃ³n de un DNI falso (8 nÃºmeros y 1 letra) y su versiÃ³n normalizada.DummyDataSeeder: Creamos un seeder que hace el trabajo pesado en segundos: crea 5 departamentos, genera 50 empleados aleatorios, les asigna el rol de employee mediante Spatie, y les crea un saldo de vacaciones (VacationBalance) con dÃ­as consumidos y arrastrados al azar.SoluciÃ³n del error SQLite: Laravel 11 viene por defecto con base de datos SQLite. Al ejecutar los seeders nos dio error. Lo solucionamos entrando al archivo .env, cambiando DB_CONNECTION=mysql y poniendo el nombre de nuestra base de datos en DB_DATABASE para que se conectara correctamente a nuestro phpMyAdmin. Finalmente, ejecutamos php artisan migrate:fresh --seed.1.15.- AuditorÃ­a y Trazabilidad (El 100% del Backend) El documento exige cumplir con normativas estrictas (como el RGPD) y tener una "caja negra" que registre quiÃ©n hace quÃ© en el sistema . Para implementar esto sin ensuciar los controladores, usamos el paquete spatie/laravel-activitylog.Entramos a los modelos clave (User, Document, Vacation) y les aÃ±adimos el trait LogsActivity.Seguridad en la inserciÃ³n: Mantenemos la protecciÃ³n $fillable (Lista Blanca) en todos los modelos para indicar quÃ© columnas se pueden rellenar de forma masiva .El truco de Spatie: En lugar de decirle a la auditorÃ­a campo por campo quÃ© debe vigilar, usamos la funciÃ³n getActivitylogOptions() con el mÃ©todo ->logFillable(). AsÃ­, Spatie lee automÃ¡ticamente nuestra lista blanca y registra cualquier cambio.TambiÃ©n usamos ->logOnlyDirty() para que solo guarde un registro si el valor realmente ha cambiado, y ->dontSubmitEmptyLogs() para no llenar la base de datos de basura. Con esto, si un administrador aprueba unas vacaciones o borra una nÃ³mina, queda grabado a fuego en la tabla activity_logs con la fecha, la hora, y el ID del usuario responsable .2.- Frontend (React & Tailwind)2.1.- InicializaciÃ³n del Frontend (Vite y Axios) Para mantener la arquitectura completamente desacoplada de la API, creamos un proyecto de React independiente utilizando Vite (npm create vite@latest) en una carpeta separada de Laravel. Instalamos dependencias clave como react-router-dom y axios. Para lograr que el frontend se comunique correctamente con la seguridad de Laravel Sanctum, configuramos una instancia global de Axios en la que activamos withCredentials: true. AdemÃ¡s, para solucionar problemas de bloqueo del token CSRF en las versiones recientes de Axios (1.x+), aÃ±adimos la configuraciÃ³n crucial withXSRFToken: true.2.2.- Ajustes CrÃ­ticos de AutenticaciÃ³n (Laravel 11) Al intentar conectar Front y Back, nos topamos con las estrictas medidas de seguridad de Laravel 11. Tuvimos que realizar varios ajustes para que el login funcionara:Login por DNI: Modificamos el AuthController para que la autenticaciÃ³n se valide usando el DNI del empleado en lugar del correo electrÃ³nico, adaptÃ¡ndolo a los requisitos lÃ³gicos de RRHH.ConfiguraciÃ³n de sesiÃ³n (.env): Ajustamos las variables para evitar que el navegador bloqueara las cookies. Establecimos FRONTEND_URL=http://localhost:5173, SANCTUM_STATEFUL_DOMAINS=localhost:5173,127.0.0.1:5173 y dejamos la variable SESSION_DOMAIN= totalmente vacÃ­a.ActivaciÃ³n de Stateful API: En Laravel 11, las rutas de la API vienen sin "memoria" por defecto. Lo solucionamos editando el archivo bootstrap/app.php y aÃ±adiendo $middleware->statefulApi() para obligar a Laravel a recordar las cookies de sesiÃ³n en las rutas API.CORS: Publicamos la configuraciÃ³n de CORS y activamos 'supports_credentials' => true para que Laravel acepte las credenciales enviadas desde el puerto de React.2.3.- Arquitectura SPA, Rutas y Estado Global Para que la aplicaciÃ³n funcione como una SPA (Single Page Application) real y no pierda la sesiÃ³n del usuario al recargar la pÃ¡gina, implementamos dos sistemas:Context API (AuthContext): Creamos un "cerebro global" que se encarga de preguntar a Laravel (mediante /api/v1/auth/me) si la sesiÃ³n sigue activa nada mÃ¡s abrir la pÃ¡gina. Guarda los datos del usuario logueado para que cualquier componente pueda leerlos.React Router: Separamos la aplicaciÃ³n en vistas. Creamos rutas pÃºblicas (como el /login) y rutas protegidas. Para estas Ãºltimas, programamos un componente ProtectedRoute que actÃºa como "guardaespaldas", expulsando al Login a cualquier usuario que intente entrar al Dashboard sin estar autenticado.2.4.- DiseÃ±o y MaquetaciÃ³n (Tailwind CSS y resoluciÃ³n de bugs) Para darle un aspecto profesional y corporativo a la aplicaciÃ³n, integramos Tailwind CSS. Durante la instalaciÃ³n nos encontramos con varios problemas tÃ©cnicos:Error de ejecutable NPM: El comando npx tailwindcss init fallaba por la cachÃ© de NPM. Lo solucionamos creando a mano los archivos tailwind.config.js y postcss.config.js.Incompatibilidad Tailwind v4: Vite lanzaba un error de PostCSS al arrancar. Descubrimos que NPM nos habÃ­a instalado la recientÃ­sima versiÃ³n 4 de Tailwind, la cual usa una arquitectura distinta. Lo arreglamos haciendo un downgrade forzado a la versiÃ³n estable 3 (npm install -D tailwindcss@^3.4).DiseÃ±o Final: Implementamos una interfaz "Premium". Creamos un Login a pantalla completa con un fondo degradado oscuro elegante y una tarjeta blanca con inputs de alto contraste. Para el Dashboard, sustituimos el diseÃ±o bÃ¡sico por un panel de control avanzado con un menÃº lateral oscuro (Sidebar), tarjetas redondeadas para los datos del usuario y alertas condicionales exclusivas para los administradores.
