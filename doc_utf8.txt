activacion practicosactivacion practicosProyecto propuesto en las prßcticas de Globomatik para recursos humanosProyecto propuesto en las prßcticas de Globomatik para recursos humanosPortal de Gesti¾n de RRHHProyecto tochoPortal de Gesti¾n de RRHHProyecto tochoContenidoNo se encontraron entradas de tabla de contenido.1.- Backend(Tablas) -Users-departments-roles-documents-vacations-holiday_dates-vacation_balances-activity_logs Creamos los modelos de las tablas principales con el comando de siempre, con -mc para las migraciones y los controles y al final a±adimos ûapi para generar los controladores sin las vistas HTML(crÚate, edit), preparßndolo exclusivamente para devolver un JSON. Ej: php artisan make:model VacationBalance -m -c ûapi Como me recomiendan paquetes externos (Roles y logs) tenemos que hacerlo de otra manera porque me pide usar paquetes de Spatie. No se crean los modelos a mano, sino que se instalan paquetes y publicar sus tablas Para roles y permisos composer require spatie/laravel-permission php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider" Para los logs de auditorÝa composer require spatie/laravel-activitylog php artisan vendor:publish --provider="Spatie\Activitylog\ActivitylogServiceProvider" --tag="activitylog-migrations" 1.1.- Uso de Spatie (Explicaci¾n IA) spatie/laravel-permission (Gesti¾n de Roles y Permisos) En lugar de que t· tengas que programar desde cero la l¾gica de "quiÚn puede hacer quÚ" y crear m·ltiples tablas complejas, este paquete te lo da hecho. Para quÚ sirve en tu proyecto: Te permite asignar autorizaciones nativas con los roles requeridos: admin, hr_director, y employee. Tablas que genera: Crea automßticamente las tablas roles y model_has_roles (entre otras). Ejemplo prßctico: Gracias a esto, con una simple lÝnea de c¾digo como $user->hasRole('hr_director') podrßs verificar si un usuario tiene permiso para aprobar o rechazar vacaciones. spatie/laravel-activitylog (AuditorÝa y Trazabilidad) El documento exige cumplir con normativas estrictas (como el RGPD), por lo que necesitas saber exactamente quÚ hace cada usuario en el sistema. Para quÚ sirve en tu proyecto: Act·a como una "caja negra". Guarda un registro (Activity log) de todas las acciones importantes para tener trazabilidad. Tablas que genera: Crea automßticamente la tabla activity_logs para la auditorÝa. Ejemplo prßctico: Si un administrador sube una n¾mina, o un Director de RRHH aprueba unas vacaciones, este paquete anota en la base de datos: QuiÚn lo hizo, cußndo lo hizo, y quÚ datos cambiaron exactamente. En resumen: Spatie te evita reinventar la rueda . Al usar sus paquetes, delegas la seguridad de los roles y la complejidad de la auditorÝa a c¾digo que ya ha sido probado por miles de empresas en todo el mundo. 1.2.- Relleno de migraciones y subida a la BD Rellenamos las migraciones seg·n nos pide el pdf. Cuando las acabamos debemos de linkear la BD con nuestro proyecto y posteriormente hacer un migrate:fresh de esos. Cuando veamos las tablas en phpMyAdmin ya estarÝa eso. 1.3.-Models Cuando creamos las tablas con sus columnas en las migraciones debemos de poner los campos en los modelos para indicar que se pueden rellenar y tambiÚn debemos de poner el tipo de relaciones(has many y todas las paranoias esas). Mas tarde hacemos un seeder para tener un usuario admin. 1.4.-Seguimos Creando Creamos un controller para usar laravel Sanctum para las validaciones que tenemos que hacer a la hora de iniciar sesi¾n. Creamos el auth controller: php artisan make:controller Api/V1/AuthController Lo rellenamos como debemos de hacer y ponemos lo necesario en api. 1.5.- Dependencias y Configuraci¾n API-first Como el frontend va a ser totalmente desacoplado en React, no instalamos ni Livewire ni Blade. Solo instalamos paquetes centrados en la API, rendimiento y documentaci¾n:Para instalar Sanctum y generar api.php: php artisan install:apiPara documentar la API con OpenAPI: composer require --dev knuckleswtf/scribe y luego php artisan vendor:publish --tag=scribe-configPara calidad de c¾digo y detectar errores: composer require --dev barryvdh/laravel-debugbar, composer require --dev nunomaduro/larastan y friendsofphp/php-cs-fixer.1.6.- M¾dulo de Empleados (La TrÝada API-first) Para seguir el estßndar profesional API-first, dividimos la l¾gica de cada m¾dulo en 3 partes ejecutando estos comandos:FormRequest (Para validar la entrada): php artisan make:request Api/V1/StoreEmployeeRequestAPI Resource (Para formatear el JSON de salida y ocultar datos sensibles): php artisan make:resource V1/EmployeeResourceControlador API (El cerebro que conecta todo): php artisan make:controller Api/V1/EmployeeController --apiNota de resoluci¾n de errores: Al crear el borrado de empleados (destroy), fallaba el $this->authorize(). Lo solucionamos usando directamente la funci¾n de Spatie a travÚs de la Request del usuario: if (!$request->user() || !$request->user()->hasRole('admin')) { return error 403 }.1.7.- Rutas API y apiResource Actualizamos nuestro routes/api.php metiendo las rutas dentro del grupo protegido por Sanctum (Route::middleware('auth:sanctum')). En lugar de crear 5 rutas GET, POST, DELETE a mano, usamos un atajo de Laravel que genera todo el CRUD automßtico: Route::apiResource('employees', EmployeeController::class);.1.8.- Configuraci¾n de CORS Un paso crÝtico de seguridad para que el navegador no bloquee a React cuando intente conectarse al Laravel.Publicamos el archivo: php artisan config:publish corsEntramos a config/cors.php y a±adimos los paths permitidos (['api/*', 'sanctum/csrf-cookie']), los orÝgenes (http://localhost:3000 y 5173) y activamos supports_credentials => true.Finalmente, a±adimos FRONTEND_URL=http://localhost:3000 en el archivo .env.1.9.- M¾dulo de Documentos y N¾minas Aplicamos la misma "TrÝada API-first" (Request, Resource, Controller) para gestionar las n¾minas, contratos y otros certificados.Comandos base: php artisan make:controller Api/V1/DocumentController --api (junto con su FormRequest y su Resource).L¾gica de Seguridad: Los archivos PDF no se guardan en la carpeta p·blica (public/), sino en el almacenamiento privado del servidor (storage/app/documents). De esta forma, es imposible que alguien descargue una n¾mina adivinando la URL en el navegador.Para descargarlo, el frontend hace una petici¾n a la API. El controlador comprueba si el usuario logueado es el due±o del documento o si tiene rol de RRHH/Admin. Si pasa el filtro, devuelve el archivo usando Storage::download().1.10.- M¾dulo de Vacaciones (El n·cleo de negocio) Este es el m¾dulo mßs complejo ya que incluye las reglas de negocio principales.Comandos base: Generamos VacationController, StoreVacationRequest y VacationResource.Regla de solapamientos: Antes de guardar una nueva solicitud en la BD, el controlador hace una consulta para asegurarse de que el empleado no tiene otra solicitud (pendiente o aprobada) que coincida en esas fechas.Gesti¾n de estados: El empleado solicita las vacaciones y entran en estado pending. El empleado solo puede cancelar (canceled) si estßn en pendiente.Solo los usuarios con rol admin o hr_director pueden lanzar los mÚtodos de approve o reject.Descuento automßtico: Al ejecutar la aprobaci¾n, el controlador busca el saldo de ese a±o y le suma los dÝas gastados automßticamente.1.11.- M¾dulo de Saldos de Vacaciones (Vacation Balances) Este m¾dulo alimenta la interfaz de React para decirle al empleado cußntos dÝas le quedan.Truco de Laravel: En lugar de crear los saldos a mano a final de a±o, usamos el mÚtodo firstOrCreate() en el controlador. Si un empleado consulta su saldo de este a±o y no existe en la BD, Laravel lo crea en ese mismo instante asignßndole los dÝas base por convenio (ej. 22 dÝas).En el VacationBalanceResource, calculamos los dÝas reales disponibles "al vuelo" sumando los dÝas generados y los arrastrados del a±o anterior, y restando los consumidos. AsÝ React recibe el n·mero limpio y directo.1.12.- M¾dulo de Fechas Festivas (Calendario Laboral) Creamos el HolidayDateController y sus archivos asociados para que RRHH pueda registrar los dÝas festivos (ßmbito nacional, de centro o personalizado). Esto es un requisito fundamental para que, al calcular cußntos dÝas gasta una solicitud de vacaciones, el sistema sepa quÚ dÝas no debe restar del saldo (ademßs de los fines de semana).1.13.- Soluci¾n de Errores: El Controller Padre Al crear los controladores de la API dentro de Api/V1, nos daba un error de herencia (extends Controller). Esto ocurre porque en Laravel 11 el archivo controlador base a veces no se genera.Soluci¾n: Creamos manualmente el archivo raÝz en app/Http/Controllers/Controller.php definiÚndolo como una clase abstracta vacÝa (abstract class Controller {}). Con esto, todos los controladores hijos heredan correctamente y desaparecen los fallos en el c¾digo.1.14.- Generaci¾n de Datos de Prueba (Seeders y Factories) Para poder programar y probar el frontend en React (paginaci¾n, filtros, grßficas), es imposible hacerlo con una base de datos vacÝa. Para solucionarlo, automatizamos la creaci¾n de datos:UserFactory: Modificamos la fßbrica de usuarios de Laravel para que genere datos coherentes con nuestro sistema, incluyendo la generaci¾n de un DNI falso (8 n·meros y 1 letra) y su versi¾n normalizada.DummyDataSeeder: Creamos un seeder que hace el trabajo pesado en segundos: crea 5 departamentos, genera 50 empleados aleatorios, les asigna el rol de employee mediante Spatie, y les crea un saldo de vacaciones (VacationBalance) con dÝas consumidos y arrastrados al azar.Soluci¾n del error SQLite: Laravel 11 viene por defecto con base de datos SQLite. Al ejecutar los seeders nos dio error. Lo solucionamos entrando al archivo .env, cambiando DB_CONNECTION=mysql y poniendo el nombre de nuestra base de datos en DB_DATABASE para que se conectara correctamente a nuestro phpMyAdmin. Finalmente, ejecutamos php artisan migrate:fresh --seed.1.15.- AuditorÝa y Trazabilidad (El 100% del Backend) El documento exige cumplir con normativas estrictas (como el RGPD) y tener una "caja negra" que registre quiÚn hace quÚ en el sistema . Para implementar esto sin ensuciar los controladores, usamos el paquete spatie/laravel-activitylog.Entramos a los modelos clave (User, Document, Vacation) y les a±adimos el trait LogsActivity.Seguridad en la inserci¾n: Mantenemos la protecci¾n $fillable (Lista Blanca) en todos los modelos para indicar quÚ columnas se pueden rellenar de forma masiva .El truco de Spatie: En lugar de decirle a la auditorÝa campo por campo quÚ debe vigilar, usamos la funci¾n getActivitylogOptions() con el mÚtodo ->logFillable(). AsÝ, Spatie lee automßticamente nuestra lista blanca y registra cualquier cambio.TambiÚn usamos ->logOnlyDirty() para que solo guarde un registro si el valor realmente ha cambiado, y ->dontSubmitEmptyLogs() para no llenar la base de datos de basura. Con esto, si un administrador aprueba unas vacaciones o borra una n¾mina, queda grabado a fuego en la tabla activity_logs con la fecha, la hora, y el ID del usuario responsable .2.- Frontend (React & Tailwind)2.1.- Inicializaci¾n del Frontend (Vite y Axios) Para mantener la arquitectura completamente desacoplada de la API, creamos un proyecto de React independiente utilizando Vite (npm create vite@latest) en una carpeta separada de Laravel. Instalamos dependencias clave como react-router-dom y axios. Para lograr que el frontend se comunique correctamente con la seguridad de Laravel Sanctum, configuramos una instancia global de Axios en la que activamos withCredentials: true. Ademßs, para solucionar problemas de bloqueo del token CSRF en las versiones recientes de Axios (1.x+), a±adimos la configuraci¾n crucial withXSRFToken: true.2.2.- Ajustes CrÝticos de Autenticaci¾n (Laravel 11) Al intentar conectar Front y Back, nos topamos con las estrictas medidas de seguridad de Laravel 11. Tuvimos que realizar varios ajustes para que el login funcionara:Login por DNI: Modificamos el AuthController para que la autenticaci¾n se valide usando el DNI del empleado en lugar del correo electr¾nico, adaptßndolo a los requisitos l¾gicos de RRHH.Configuraci¾n de sesi¾n (.env): Ajustamos las variables para evitar que el navegador bloqueara las cookies. Establecimos FRONTEND_URL=http://localhost:5173, SANCTUM_STATEFUL_DOMAINS=localhost:5173,127.0.0.1:5173 y dejamos la variable SESSION_DOMAIN= totalmente vacÝa.Activaci¾n de Stateful API: En Laravel 11, las rutas de la API vienen sin "memoria" por defecto. Lo solucionamos editando el archivo bootstrap/app.php y a±adiendo $middleware->statefulApi() para obligar a Laravel a recordar las cookies de sesi¾n en las rutas API.CORS: Publicamos la configuraci¾n de CORS y activamos 'supports_credentials' => true para que Laravel acepte las credenciales enviadas desde el puerto de React.2.3.- Arquitectura SPA, Rutas y Estado Global Para que la aplicaci¾n funcione como una SPA (Single Page Application) real y no pierda la sesi¾n del usuario al recargar la pßgina, implementamos dos sistemas:Context API (AuthContext): Creamos un "cerebro global" que se encarga de preguntar a Laravel (mediante /api/v1/auth/me) si la sesi¾n sigue activa nada mßs abrir la pßgina. Guarda los datos del usuario logueado para que cualquier componente pueda leerlos.React Router: Separamos la aplicaci¾n en vistas. Creamos rutas p·blicas (como el /login) y rutas protegidas. Para estas ·ltimas, programamos un componente ProtectedRoute que act·a como "guardaespaldas", expulsando al Login a cualquier usuario que intente entrar al Dashboard sin estar autenticado.2.4.- Dise±o y Maquetaci¾n (Tailwind CSS y resoluci¾n de bugs) Para darle un aspecto profesional y corporativo a la aplicaci¾n, integramos Tailwind CSS. Durante la instalaci¾n nos encontramos con varios problemas tÚcnicos:Error de ejecutable NPM: El comando npx tailwindcss init fallaba por la cachÚ de NPM. Lo solucionamos creando a mano los archivos tailwind.config.js y postcss.config.js.Incompatibilidad Tailwind v4: Vite lanzaba un error de PostCSS al arrancar. Descubrimos que NPM nos habÝa instalado la recientÝsima versi¾n 4 de Tailwind, la cual usa una arquitectura distinta. Lo arreglamos haciendo un downgrade forzado a la versi¾n estable 3 (npm install -D tailwindcss@^3.4).Dise±o Final: Implementamos una interfaz "Premium". Creamos un Login a pantalla completa con un fondo degradado oscuro elegante y una tarjeta blanca con inputs de alto contraste. Para el Dashboard, sustituimos el dise±o bßsico por un panel de control avanzado con un men· lateral oscuro (Sidebar), tarjetas redondeadas para los datos del usuario y alertas condicionales exclusivas para los administradores.
